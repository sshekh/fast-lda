7) All the other optimizations: ILP, scalar replacement, register level optimizations

9) Use _mm_malloc to force alignment and then only use aligned loads/stores. <- ??

10) i. Finalize log_sum (lookup/expansion + vectorization) <- BG
   ii. compute likelihood lgamma loop vectorize using mkl, tiling <- BG
  iii. optimize digamma (lookup/approx + vectorization) <- lookup <- CC, approx <- SS then vectorize
   iv. Tiling inference <- FL
    v. Bugfix pure computation chart <- SS
   vi. Plots (roofline, performance, cycle distribution) for each stage <- FL
  vii. Optimize lgamma (lookup/approx + vectorization) <- FL        //Low priority